# 바이트론 이앤에스 업무 현황 대시보드

직원별 일간/주간/월간/연간 업무 기록 및 Jira 연동 대시보드.
2026-08-15 부터 **일정 관리(스케줄)** — 사람 위치·차량 예약·이동 비용 정산 — 도 함께 담는다.

## ⚠️ 대화·작업 규칙 (최우선 — 반드시 준수)

이 규칙은 기기·세션·경로와 무관하게 항상 적용된다. (이 파일이 git으로 모든 기기에 동기화되므로 새 기기·새 로그인에서도 자동 적용)

1. **말투 — 항상 공손한 존댓말.** 반말("~할게", "~할까?", "~이야") 절대 금지. 문장 종결은 "~합니다 / ~하겠습니다 / ~할까요?" 형태로.
2. **파일 편집 전 반드시 확인.** 아무리 간단한 수정이라도 계획·원인을 먼저 설명하고 "진행할까요?"로 동의를 받은 뒤에만 Edit/Write를 사용한다. "간단해서", "명백한 버그라서" 같은 예외 없음.
   - **승인 게이트(2026-07-17 추가)**: Write/Edit/커밋/push 직전에 반드시 자문한다 — *"직전 사용자 메시지에 명시적 실행 지시(하자/해줘/진행/응/번호 선택)가 있는가?"* 없으면(질문·감상·설명이면) **도구를 쓰지 않고 답변만 하고 턴을 끝낸다.** 사용자의 질문은 승인이 아니라 추가 질문이다. 제안으로 끝나는 턴은 반드시 도구 호출 없이 끝난다. 승인 1번은 그 작업 1건에만 유효하며 다음 작업으로 확대 적용하지 않는다.
3. **수정 후 동작 검증 후 보고.** 브라우저/앱에서 실제 동작을 확인한 뒤 결과를 보고한다.
4. **수정 완료 후 push.** 작업이 끝나면 `git add` → `commit` → `push` 까지 수행한다.
5. **경로는 «전체 경로»로 말한다.** `docs/manual` 처럼 줄여 쓰지 않는다 —
   저장소가 여러 개라 어느 것인지 알 수 없고 탐색기에 붙여 넣지도 못한다 (2026-08-24 지시).
6. 🔑**문서를 만들면 공용 폴더에도 «항상» 같이 놓는다** (2026-08-24 지시).
   ```powershell
   & "D:\My Projects\Vitron\PUBLIC\REPORT_DASHBOARD\20260526\REPORT_DASHBOARD\vitron-dashboard\tools\manual\sync-docs.ps1"
   ```
   - 사본 자리 = **`D:\Vitron\DASHBOARD + KPI`** (대시보드·KPI 두 제품이 함께 들어간다)
   - 대상 = `docs\manual\*` · `docs\testcase\*` 중 **docx·pdf·xlsx·pptx** 만
     (같은 폴더의 작업 메모 `.md` 는 옮기지 않는다)
   - **하지 말라는 말씀이 없으면 매번 돌린다.** 매뉴얼·TC 를 새로 생성했으면 그 자리에서
   - 날짜가 아니라 **SHA256** 으로 대조한다 → [[feedback-deploy-folder-sync]] 와 같은 원칙

## 기술 스택
- **Frontend**: React + Vite (단일 파일 구조 — `src/App.jsx`)
- **Backend**: Node.js + Express (`server/index.js`)
- **DB**: 사내 PostgreSQL (`vitron-nas:5432`, DB `vitron_dashboard`)
- **배포**: 사내 NAS Docker (`http://vitron-nas:8082`) — 아래 배포 절차 참고
- **저장소**: 사내 Gitea 주 저장소 + GitHub 백업 (`git push` 한 번에 양쪽 전송)
  - 주: `http://vitron-nas:8084/GunhoLee/vitron-dashboard` (origin)
  - 백업: `https://github.com/GUNHOLEE-KR/vitron-dashboard` (origin push 대상에 포함, 별칭 `github`)
  - ⚠️ Git 저장소는 **배포 경로에 없다.** `push-to-nas.ps1` 이 로컬 커밋을 직접
    tar 로 묶어 NAS 에 보내므로, push 하지 않아도 배포되고 저장소를 옮겨도 배포는 영향 없다

> Supabase·Vercel 은 2026-06-02 에 걷어냈다. `supabase/`·`api/` 폴더는 잔재이며 사용하지 않는다.

## 주요 파일 구조
```
src/
  App.jsx                  # 전체 UI (컴포넌트 분리 없이 단일 파일)
  repositories/            # 백엔드 REST API 호출 (fetch)
    workerRepo.js          # 직원 CRUD
    historyRepo.js         # 업무 기록 CRUD
    jiraRepo.js            # Jira 동기화·이슈 관리·토큰 만료 조회
    scheduleRepo.js        # 일정·장소·차량·실적·정산·로그인
    profitRepo.js          # 프로젝트 손익 — 계약금액·인건비 단가·손익표 (대표이사 전용)
    expenseRepo.js         # 출장 경비 + 카카오톡 보내기·연결 상태
server/
  index.js                 # Express REST API + Jira 동기화 (단일 파일)
  kakao.js                 # 카카오 HTTP 만 (동의 주소 · 코드 교환 · 갱신 · 나에게 보내기)
db/init.sql                # PostgreSQL 스키마 (기존 테이블 3개)
db/migrations/             # 이후 변경 SQL — 001~048 (003 은 롤백용도 함께 둠)
                           # ✅ 001~048 모두 «운영·테스트 두 DB 에» 들어가 있다
                           #   (044~048 은 2026-09-25 운영 배포 직전에 넣음). 다시 돌리지 말 것
                           # 🔑 여기 적힌 말을 믿지 말고 «DB 에 물어볼 것» — 이 줄이
                           #   실제로 낡아 있던 적이 있다(031·032 가 「테스트에만」 이라
                           #   적혀 있었으나 운영에 이미 들어가 있었다).
                           #     SELECT column_name FROM information_schema.columns
                           #      WHERE table_name='…' ;
                           # 🔴 새 마이그레이션은 «배포보다 먼저» 운영 DB 에 넣는다 —
                           #   안 넣고 배포하면 새 API 가 500 으로 죽는다
Dockerfile.frontend        # React 빌드 → nginx
Dockerfile.backend         # Node.js API 서버
nginx.conf                 # /api/* → backend 프록시, 타임아웃 300초
docker-compose.yml         # 프론트(8082) + 백엔드 2개 컨테이너
deploy.sh                  # NAS 배포 스크립트
```

## PostgreSQL 테이블
- `workers` — 직원 정보 (name, active, hired_at, resigned_at, email, **position**)
- `work_history` — 업무 기록 (worker_name, work_date, work_hour, work_text)
- `jira_issues` — Jira 이슈 캐시 (jira_key, summary, parent_key, full_text, **status_name, status_category**)
- `worker_absences` — 집계 제외 기간 (아래 절 참고)
- `holidays` — 공휴일표 (하루 1회 자동 수신 · 「그날 근무」로 되돌릴 수 있다)
- `schedule_places` / `place_distances` / `schedule_vehicles` / `schedule_plans` /
  `schedule_actuals` / `schedule_settlements` — 일정 관리 (아래 절 참고)
  - `place_distances` 는 «장소 쌍» 거리다. 장소의 `distance_km` 는 「회사 → 장소」
    하나뿐이라 현장끼리의 거리(「이동」)를 담을 곳이 없었다
- `hipass_uploads` / `hipass_tolls` — 하이패스 사용내역 (아래 절 참고)
- `meetings` / `agenda_items` / `meeting_notes` — 회의록·안건·참석자별 발표 내용 (아래 절 참고)
- `purchase_requests` — 구매 요청·승인·이력
- `vehicle_events` / `vehicle_insurances` — 차량 정비·검사·보험 (아래 절 참고)
- `trip_expenses` — 출장 경비(법인카드 사용 내역) (아래 절 참고)
- `mail_account` / `mail_senders` — 회사 공용 메일 계정 · 사람별 발송 계정
- `project_contracts` / `worker_hourly_rates` — 프로젝트 계약금액 이력 · 사람별 시간당 단가 (아래 절 참고)
- `kakao_links` — 카카오톡 받는 사람의 연결(토큰은 봉해서) (아래 절 참고)
- `kpi_*` — KPI 추적 시스템과 **같은 DB 를 함께 쓴다** (계정 `kpi_users` 포함)

⚠️ `DATE` 타입은 `pg` 가 Date 객체로 바꿔 프론트의 `slice(0,7)` 를 깨뜨린다.
`server/index.js` 에서 `types.setTypeParser(1082, …)` 로 문자열 유지 중이니 제거하지 말 것.

⚠️ `NUMERIC` 도 마찬가지로 문자열로 온다. `types.setTypeParser(1700, parseFloat)` 가 없으면
거리 합산이 덧셈이 아니라 **이어붙이기**(`100.0`+`50.0`=`100.050.0`)가 되어 정산 금액이 무너진다.

## 일정 관리 (스케줄)
사람 위치 파악 · 회사 차량 예약 · 이동 비용 정산. 월/주/일/연 달력 + 정산 화면.

- 유형 3가지 = 업무 / 차량 개인사용 / 휴가. **유형이 저장할 칸을 결정**한다 —
  개인사용의 장소·목적은 화면에서 묻지 않을 뿐 아니라 **서버가 받아도 지운다**(사생활)
- 🔑**장소가 이동 수단을 결정한다.** 이동 수단 목록에 「사무실」을 두지 않는다 —
  고를 수 없으면 장소와 어긋날 수 없다 (이 어긋남으로 내근 등록이 막힌 적 있음)
- 거리·소요시간은 **수기 입력**(지도 API 는 비용 때문에 쓰지 않기로 결정). 장소에 저장해 두고 자동 채움
- 🔴**지도 링크는 «네이버»** (2026-09-06). 구글은 **한국 국내 «자동차» 길찾기를 아예 제공하지 않아**
  종전 `travelmode=driving` 링크는 국내 구간에서 한 번도 열린 적이 없었다
  (「운전 경로 검색 서비스 범위를 벗어난 검색입니다」).
  ⚠ **이름만으로 출발지·도착지가 채워지는 링크는 어디에도 없다** — 카카오는 `sName`·`eName` 을
  통째로 버리고(실측), 네이버는 좌표를 요구한다. 그래서 «길찾기 화면까지» 열어 주고
  이름은 **[📋 복사] 단추**로 집어 붙여 넣게 했다
  - ⚠ 사내는 `http` 라 `navigator.clipboard` 가 **아예 없다**(보안 컨텍스트가 아니다).
    `execCommand` 옛 방식을 뒤에 두었고, **clipboard 가 «있어도 거부되면»** 그리로 넘어간다.
    🔑 `copyText` 는 **절대 던지지 않는다** — 처음 판이 promise 를 그대로 돌려주는 바람에
    거부될 때 `await` 가 튕겨 「복사도 안 되고 알림도 안 뜨는」 상태가 됐다(실측)
- 🔑**편도는 방향을 고른다** — 출발(🏢→) / 복귀(→🏢) / **이동(📍→📍)**.
  「이동」은 사무실을 거치지 않는 하루라 **출발지(`from_place_id`)를 따로 받는다**.
  거리는 `place_distances`(장소 쌍)에서 오고, 없으면 한 번 적어 두면 다음부터 채워진다
- ⚠ 자차는 **본인 차량만** 고를 수 있다 (남의 차로 환급이 잡히면 안 된다)
- 정산 = 개인사용 `거리×단가+하이패스` 청구 / 자차 `거리÷연비` **리터**로 환급 / 대중교통 실비
- 🔑**정산은 «사람마다 · 세 단계»** (2026-09-03·09-04) — `open` 미정산 →
  `notified` 입금 대기 → `settled` 완료
  - **1차 안내** = 금액 박제 + 실적 `locked` + **본인에게 «자기 정산 내역» 메일**.
    입금할 것이 없는 사람은 그 자리에서 `settled` 로 넘어간다
  - **2차 입금 확인** = 완료로 넘긴다. **메일은 가지 않는다**
  - 잠긴 뒤 수정은 409 → 대표이사가 잠금 해제 후 재승인
  - 확정 뒤 값이 달라지면 화면에 「→ 현재」로 함께 적고, **합계는 확정 금액 기준**
- ⚠️ 이동 수단을 바꾸면 **차량 선택을 비울 것** — 남아 있으면 자차 일정이 회사 차량을 잡아 겹침 오탐
- 🔑**계획의 「업무」 칸 = 프로젝트**(2026-09-25, 마이그 `045`·`046`). 「무엇을 하러」 글과
  프로젝트를 두 번 묻던 것을 한 칸으로 합쳤다(지시)
  - `schedule_plans.projects` JSONB = `[{parent_key,parent_text,share}]` 또는
    `{free:true,label,share}`(「✎ 직접 적기」 — 프로젝트가 아닌 일도 있다, 지시) · 최대 10줄
  - 한 번 나가 여러 곳 → **비율**. 서버 `cleanProjects` 가 큰 나머지 방식으로 **정수 100%**
    에 맞춘다(막지 않는다). 0% 줄은 버린다
  - `work_note` = 세부 **메모**(사용자 선택 (나)). `purpose` 는 «프로젝트 이름 · 메모» 로
    화면이 만들어 «저장»한다 — 달력·메일은 예전처럼 `purpose` 만 읽는다
  - 🔑예전 계획의 `purpose` 는 **메모로 읽는다**(`planMemo`) — 열었다 닫기만 해도 지워지면 안 된다
  - 🔑`PATCH /api/schedule/plans/:id/projects` — **잠긴 계획에도** 프로젝트·메모만 고친다
    (정산 금액에 닿지 않는다). 주인·관리자·**대표이사**
  - 업무가 아니면(개인 사용·휴가) 서버가 `projects`·`work_note` 를 비운다
- 문서 = Confluence 「일정 관리」(부모 170164456, 하위 6쪽) / Jira VITRON-317·318

### 휴가는 «시간» 으로 센다 (2026-09-07, 마이그 `033`·`035`)
「종일 1일 / 반차 0.5일」 두 값뿐이라 반차보다 짧게 쓰는 일을 담을 수 없었다.

- **1휴가 = 8시간 · 1시간 단위.** 시작·끝 «시각» 을 받아 시스템이 센다.
  근무 `09:00~18:00` · 점심 `12:00~13:00` 은 **뺀다**. 「반차」 단추는 없앴다
- 🔑**계산해서 «저장»한다**(`vacation_hours`). 조회할 때마다 다시 세면 나중에 근무·점심
  시각을 고치는 순간 **이미 결재된 지난 휴가의 시간까지 소급해 달라진다**
- 상수는 한 자리씩만 — 서버 `WORK_TIME` · 화면 `VAC_WORK`
- 연차 집계·메일은 **일과 시간을 함께** 적는다(「17.1일 · 137시간」)
- **종류 = 연차 / 병가 / 포상 / 「공가」 / 기타** + **사유 칸**(`vacation_note`).
  🔑「공가」는 예비군·경조처럼 «업무가 아니지만 연차에서 깎으면 안 되는» 것이다
  (2026-09-07 지시). 달력에 **🏛**. 🔴사유를 «종류» 에 담지 않는다 —
  종류에 「예비군」을 넣으면 집계가 갈라져 「기타」 합계를 셀 수 없다
- **승인권자가 여럿이다** — 신청·취소 메일은 `can_approve_settlement` 인 **전원**에게,
  승인·반려 결과는 **신청자 + 처리한 사람을 뺀 나머지 승인권자**에게 간다(`approverEmails`)

### 🔑 «지난 날짜» 휴가·공가는 신청이 아니라 «정리» 다 (2026-09-10 지시)
지나간 날을 뒤늦게 적어 넣는 것은 결재할 것이 없다. **실적 완료 보고를 「당일 것만
보낸다」로 둔 것과 같은 결**이다(`notifyDone` 의 `report_skip='late'`).

- 판정 = `plan_date < todayLocal()`. **신청·취소 메일을 보내지 않고** 그 자리에서 승인된 것으로 넣는다
- 🔴**그때 `approved_by_id` 는 «비운다»** — 사람이 승인한 것이 아니기 때문이다.
  마이그레이션 없이 이 한 칸으로 셋이 갈린다:
  ```
  approval NULL        = 승인 제도 밖 (업무 일정 · 제도 이전 기록)
  approved + by 있음   = 사람이 승인했다 (승인 화면 또는 결재자 본인 등록)
  approved + by 없음   = «지난 날짜 정리» (자동)
  ```
- ⚠**취소 메일도 막는다** — 신청 메일이 애초에 안 갔는데 취소만 가면 받는 쪽은
  «없던 신청» 이 취소됐다고 읽는다
- 안 보낸 사실은 **로그에 남긴다**(`[mail] skip(backfill) …`). 조용히 넘기면
  「왜 메일이 안 왔지」를 나중에 답할 수 없다
- 화면은 **날짜가 섞였을 때 갈라서 묻는다** — 「지난 날짜 N건은 정리 기록이라 메일 없이
  바로 승인되고, 나머지 M건만 신청 메일이 갑니다」. 한 줄로 뭉뚱그리면 어느 쪽이
  갔는지 알 수 없다
- ⚠ 계획을 **고쳐서** 날짜를 과거로 옮기는 경우는 손대지 않았다(그 경로는 원래 휴가
  메일을 보내지 않는다). 승인 상태도 그대로 둔다

### 법인차량 «동승» (2026-09-14, 마이그 `039`)
한 차로 여럿이 가는 날을 적을 자리가 없어, 함께 타려고 둘 다 등록하면 겹침 경고가 떴다
(운영 9/16·17 윤인철·이건호 Model Y).

- `schedule_plans.carpool_group` — 함께 타는 계획들이 같은 값(처음 잡은 계획 번호).
  🔑**대표 = 살아 있는 구성원 중 번호가 가장 작은 계획 = 먼저 예약한 사람**(사용자 결정).
  FK 없음 — 대표를 지워도 묶음은 남고 다음 사람이 대표가 된다
- 🔑**겹침은 «배차 단위» 로 센다**(`carUnits` — `schedule-core.js`). 달력 위 경고 · 배차표 ·
  일 보기 「차량 배정」 이 모두 이것을 쓴다. ⚠ 공용 조각이라 포털이 칸을 안 주면 예전과 똑같다
- 등록 409 → **[함께 타기] / [따로] / [취소]** (`askChoice` — confirm 두 번으로 나누지 않는다).
  겹친 상대가 **한 묶음(또는 한 사람)** 일 때만 함께 탈 수 있다
- 이미 넣은 계획은 계획 창 **[👥 함께 타기] / [동승 풀기]** (`POST|DELETE /api/schedule/plans/:id/carpool`)
- 🔴**날짜·차량을 바꾸면 서버가 묶음에서 뺀다** — 남기면 다른 날 예약이 겹침 검사에서 조용히 빠진다
- 🔑**차량 거리·하이패스는 대표만**(사용자 결정) — `buildSettlement` 의 차량별 합계와
  하이패스 후보(`day_actuals`)에서 동승자를 뺀다. 사람별 업무 거리는 그대로
- 🔴`CARPOOL_RIDER_SQL` 은 `IS NOT NULL AND EXISTS` 로 연다 — 계획 없는 실적에서 NULL 로
  떨어지면 `NOT` 이 NULL 이 되어 하이패스 후보가 조용히 사라진다(이 저장소 세 번째 3값 논리 함정)
- 🔴잠긴 실적이 붙어 있으면 묶기·풀기를 409 로 막는다 — 대표가 바뀌면 정산 근거가 바뀐다
- 메일 = 함께 타기도 보내되 제목 **「[차량] 동승 · … · 함께: 대표」**. 풀기는 보내지 않는다

## 하이패스 사용내역 (2026-09-04·05 신설)
법인차량 통행료를 엑셀로 받아 **실적에 붙여** 정산에 넣는다. 올리는 자리는
**설정 탭이 아니라 «정산 화면»** 이다 (쓰는 자리에서 올려야 한다).

- 표 = `hipass_uploads`(올린 파일 원본 — 근거 자료로 보관·내려받기) / `hipass_tolls`(통행 한 줄씩)
- 🔑**대조** — 통행마다 «그날 그 차를 쓴 실적» 을 맞대 `입금 대상` / `환급 대상` /
  `회사 부담` / `골라야 함` / `해당 없음` 을 붙인다. 서버가 `tollHint` 로 준다
- 🔴**대조는 «참고» 다.** 출장 당일 아침에도 출퇴근 통행이 찍히므로 같은 날이라고
  업무 통행인 것이 아니다 — **마지막 선택은 사람이 한다**. 화면에도 그렇게 적어 두었다
- 「내가 그날 쓴 것만」 으로 직원이 자기 것만 추려 본다
- ⚠**실적에 붙은 줄은 지워지지 않는다** — 그 실적의 주인이 먼저 떼어야 한다.
  지운 줄은 **같은 파일을 다시 올리면 되살아난다**(`idx_hipass_dedup` 로 겹침도 막는다)
- 🔑**하이패스 개인카드** — 통행료를 «주 사용자 개인 카드» 로 내는 차
  (카니발 24구 7598·윤기곤 / QM6 129누 9183·이건호). 그 차의 통행료를 회사가
  **주 사용자에게 지급**한다(정산표 「하이패스 대납」). 남이 개인 사용한 몫은 그대로 청구.
  체크칸은 정산 화면 「차량 단가·연비」 카드 — **대표이사만**

### 배정은 «자동» 이 기본, 손은 «고치는 데» 만 (2026-09-06 지시, 마이그레이션 031·032)
사용자 정리 — 「엑셀 파일과 직원이 입력한 스케줄로 **자동 배정** 되는 게 기본이다.
스케줄을 잘못 넣거나, 다른 직원에게 보내야 하거나, 청구·정산할 필요가 없을 때
**생성된 목록을 수정**하는 기능이 필요하다」

```
① 올리기 → ② 자동 배정 → ③ 목록 고치기 → ④ 건별 메일 → ⑤ 직원 확인 → ⑥ 확정 → ⑦ 월 정산
```
- 🔑**자동 배정** = 그날 그 차를 쓴 실적이 «정확히 하나» 일 때만 붙인다.
  **둘 이상이면 사람에게 남긴다** — 025 의 「사람이 골라 붙인다」가 걱정한 것이 그 경우뿐이다.
  올릴 때 저절로 돌고(한 파일이 여러 달에 걸치므로 달마다), `[⚙ 자동 배정]` 로 다시 돌린다
  - ⚠ 손대지 않는 것 — 이미 붙은 것 · 손으로 건 것 · 제외한 것 · 확정된 것 · **잠긴 실적**
    (정산 안내가 나간 달에 붙이면 알린 금액과 근거가 어긋난다)
  - `auto_assigned` 로 「기계가 붙였다」를 남긴다. 사람이 고치면 꺼져 자동이 다시 안 건드린다
- **목록 고치기** — 다시 배정 / 다른 직원에게(`billed_worker_id`+`billed_kind`) / 정산 제외.
  권한은 **관리자·대표이사·«본인만»**. **최종 확정은 대표이사만** (지시)
- 🔑**「정산 제외」는 지우기가 아니다**(`excluded`). 기록·근거·「누구 것이었나」는 남고
  **금액 합산에서만** 빠진다. 지우면 근거가 사라지고 같은 파일을 다시 올리면 되살아난다
  - **반려도 «제외»로 간다** — 실적에서 떼지 않는다(전에는 떼서 누구 것이었나가 사라졌다)
- 🔴**같은 돈을 두 번 세지 않게** `CHECK (actual_id IS NULL OR billed_worker_id IS NULL)` —
  통행은 셋 중 하나다: 실적에 붙었거나 / 손으로 걸었거나 / 아직 아무것도
- 🔴🔴**CHECK 에 `IN` 만 쓰면 «안 막힌다»** (2026-09-06 실측으로 잡음).
  `billed_kind` 가 NULL 이면 `NULL IN (…)` 은 FALSE 가 아니라 **NULL** 이고,
  CHECK 는 **FALSE 일 때만** 막으므로 그대로 통과한다. 그러면 「방향 없는 청구」가 생기고
  정산이 그것을 조용히 **입금**으로 센다 → `coalesce(billed_kind,'')` 로 고쳤고,
  **서버 조회에서도 `billed_kind IN (…)` 으로 한 번 더 거른다**
- 🔑**정산 계산에 «두 갈래»가 더 얹힌다** — `buildSettlement` 는 실적을 훑으므로
  ①대납(`cardOwed`) ②손으로 건 것 은 거기 안 잡힌다. 뒤에서 따로 더하고,
  **그달 실적이 없는 사람은 자리를 만들어 준다** (안 만들면 청구가 조용히 사라진다)
- 화면 — 「어디로」 열은 **구간(요금소)과 다르다**. 실적의 장소 + `왕복`/`편도 출발`/
  `편도 복귀`/`현장 간 이동` 을 함께 보인다.
  ⚠ 왕복·방향·출발지는 **`schedule_plans` 에만** 있어 `plan_id` 를 거친다
  (계획 없는 실적이면 안 나온다 — 운영 실적 89건은 전부 계획을 달고 있다)
- 「내 하이패스」는 **정산 화면 안**에 둔다 — 금액과 근거가 한 화면에 있어야 이어진다

## 차량 관리 — 정비·검사·보험 (2026-09-24 신설, 마이그 `041`)
`schedule_vehicles` 에는 «정산에 필요한 것» 만 있었다(단가·연비·하이패스·색).
정비를 언제 했는지, 보험이 언제 끝나는지 적을 자리가 없어 모르고 있다가 지나갔다.

- 표 = `vehicle_events`(정비·정기검사·주유·세차·사고·기타) / `vehicle_insurances`
- 자리 = [설정] 탭 「차량 정비·보험」 카드. **법인차량만**(사용자 결정)
- 필수는 **차량과 날짜** 뿐이고 나머지는 자유 입력(지시) —
  적을 것이 마땅치 않아 «기록 자체를 안 남기는» 것이 가장 나쁘다
- 🔑**종류를 «사유» 로 쓰지 않는다** — 「엔진오일」을 종류에 넣으면 집계가 갈라져
  「정비」 합계를 셀 수 없다. 구체적인 것은 `title` 에 적는다(휴가 종류와 같은 원칙)
- **알림** = 기한 30일 전부터, **대표이사 + 그 차의 «주 사용자»**(`assigned_worker_id`).
  하루 1회 점검(공휴일 동기화와 같은 자리·같은 원칙 — 실패해도 서버를 안 멈춘다)
- 🔑**알린 뒤 «그 기한» 을 적어 둔다**(`alerted_for`). 날짜만 적으면 30일 내내 매일
  보내고, 「보냄」 표시만 두면 기한을 미뤘을 때 다시 알리지 못한다.
  `IS DISTINCT FROM` 으로 비교한다 — NULL 끼리도 제대로 갈린다
- ⚠ 메일이 실패해도 `alerted_for` 를 적는다. 안 적으면 메일 서버가 막힌 동안
  날마다 쌓였다가 살아나는 순간 한꺼번에 쏟아진다. 화면의 띠가 대신 말한다
- 보험은 **그 차의 «가장 늦은 것» 만** 알린다 — 작년 것까지 알리면 해마다 쌓여
  「이미 갱신했는데 왜 또」 가 된다
- ※ 백로그의 「법인차량 등록 화면」은 **2026-08-29 에 이미 만들어져 있었다**(확인함)

## 출장 경비 — 법인카드 사용 내역 (2026-09-24 신설, 마이그 `043`)
출장에서 «이미 쓴» 돈의 기록. 탭 키 = `expense`(표기 「경비」).

- 🔴**월 정산(`schedule_settlements`)에 «합치지 않는다»**(사용자와 합의).
  저것은 「회사에 입금 / 회사가 환급」이고 이것은 「이미 나간 돈」이라 성격이 다르다.
  합치면 정산 금액이 틀어진다 — 여기는 **조회·집계 전용**이다
- 🔑**승인 절차가 없다**(지시). 구매 요청은 «사기 전» 결재이고 이것은 «쓴 뒤» 기록이다
- 입력은 **수동**(지시 — 카드사 엑셀 올리기 아니다). 필수는 **날짜와 금액** 뿐
- **프로젝트 = Jira 상위업무**(지시). 키와 «이름» 을 함께 담는다
  (`parent_key`+`parent_text`, `meeting_notes` 와 같은 방식) — 동기화가 제목을
  바꿔도 지난 표시가 흔들리지 않는다. **프로젝트는 선택**이다(지시)
  - ⚠ 목록은 완료된 것을 감추므로 **고른 값은 목록에 남겨** 둔다 —
    안 그러면 고르개가 조용히 빈칸이 되고 저장하는 순간 날아간다(안건·회의록에서 두 번 겪음)
- **영수증은 선택**(지시). 파일은 DB(`BYTEA`)에 담는다 — 백엔드 컨테이너에
  볼륨이 없어 파일로 두면 재배포마다 사라진다(하이패스 원본과 같은 판단)
  - 🔴**nginx 의 `client_max_body_size` 가 없으면 기본값이 1MB** 라 휴대폰 사진이
    413 으로 잘린다. `12m` 으로 넣었다 — 하이패스 엑셀은 16KB 라 걸린 적이
    없었을 뿐이다. 백엔드(`express.json`)가 12mb 라 거기 맞춘다
  - 목록은 영수증 본문을 **빼고** 준다. 안 빼면 사진 수십 장이 한 번에 실려 화면이 멈춘다
- 권한 — 보는 것은 전원, 고치는 것은 **본인만**(관리자는 대리). 남의 것도 보이는 것은
  «프로젝트별 합계» 를 함께 보기 위함이다. 판정은 «DB 의 `worker_id`» 로 한다
- 집계 = **프로젝트별 / 인원별 / 월별 / 종류별** — 담긴 것은 하나이고 «묶는 기준»만 바뀐다
  - 프로젝트를 안 고른 것도 **한 줄로 묶어** 보인다. 빼 버리면 합계가 사람별과
    안 맞아 「어디로 샜지」가 된다

### 카카오톡 — 경비 보내기 (2026-09-25, 마이그 `047`·`048`)
- 🔑**무료 방식**(카카오 로그인 API). 알림톡은 유료·문구 사전 심사라 뺐다(지시)
- 받는 사람 = **대표이사**(직책), 테스트 서버는 `.env` 의 `KAKAO_TO_TEST`(로그인 메일 — 이건호)
- 🔑**보내는 방식은 `kakaoPlan()` 한 곳에서 정한다**(048, 사용자 결정 (나))
  - `friend` = `.env` `KAKAO_SENDER`(**이건호**) 의 카카오에서 대표이사(친구)에게 «친구 메시지»
    (`talk/friends/message/default/send`) — **운영**
  - `memo` = `KAKAO_SENDER` 가 없거나 받는 사람과 같으면 「나에게 보내기」(`talk/memo/default/send`) — **테스트 서버**
    (보내는·받는 사람이 둘 다 이건호라서. 자기 자신에게는 친구 메시지를 못 보낸다)
  - 🔑**본문 줄마다 쓴 직원 이름**부터(지시) — 친구 메시지는 보낸 사람이 늘 이건호로 보이므로 본문이 말해야 한다
  - 받는 사람 찾기 = 연결 때 적어 둔 **카카오 사용자 번호**(`kakao_user_id`, `/v2/user/me`) → 보내는 사람
    **친구 목록의 uuid**. 목록은 밖으로 내보내지 않는다(`/api/kakao/check` 는 «보이는가» 만)
  - 🔴카카오 규칙(공식 문서): **받는 사람도 앱에 연결**(「친구목록」·「메시지 전송」 동의) · 심사 전엔
    받는 사람이 **앱 멤버** · **하루 30건** · 친구 목록 반영에 최대 10분. 못 찾으면 이 다섯을 사람 말로 알린다
  - ⚠ 카카오는 친구 메시지의 **일부 실패를 200** 으로 준다 → `successful_receiver_uuids` 를 직접 따진다
- 🔑**경비를 등록·수정·삭제할 때 «보내지 않는다»**(지시). 경비 탭 「미전송」 카드에서
  **체크한 것만** [보내기]. 미전송 = 안 보낸 것 + **보낸 뒤 고친 것**(`updated_at > kakao_sent_at`)
  - ⚠ 보낼 때 `updated_at` 을 건드리지 않는다 — 건드리면 방금 보낸 것이 「수정됨」 으로 되돌아온다
  - 미전송 목록은 **기간과 무관하게** 전부 — 지난달 것을 깜빡해도 남는다
- 🔴**한 통 200자** — 머리(사람·건수·합계) + 들어가는 만큼 낱건 + 「…외 N건」(`expenseKakaoText`)
- 🔑[연결] 은 **보내는 사람·받는 사람 본인**에게만 연다(`kakaoRolesOf`). 남이 연결하면 엉뚱한 계정이 쓰인다
- 토큰은 `mailcred.seal`(`MAIL_CRED_KEY`)로 봉한다. refresh 는 약 두 달 → **기동 때·하루 1회 갱신**.
  실패는 `last_error` 에 남기고 설정·경비 탭이 붉게 알린다
- `.env` = `KAKAO_REST_KEY` · `KAKAO_REDIRECT_URI`(앱에 글자 그대로 등록) · `KAKAO_CLIENT_SECRET`(선택)
  · `KAKAO_TO_TEST`(테스트만) · `KAKAO_SENDER`(보내는 사람 로그인 메일 — 운영·테스트 모두 이건호).
  **키가 없으면 기능만 꺼진 채 돈다**(409 · 「⏸ 아직 꺼져 있습니다」)
- ✅ **「나에게 보내기」 실제 발송 확인**(2026-09-25, 테스트 서버) — 카카오 앱 = **DarkHorseHelper**(ID 1454727, 비즈 앱).
  Redirect URI 는 REST API 키 카드의 「로그인 리다이렉트 URI」 에 등록한다 — **8092 는 실제 연결로 확인**,
  8082 는 함께 등록하도록 안내했으나 **운영 연결 때 확인할 것**(안 돼 있으면 KOE006).
  🔑클라이언트 시크릿은 새 콘솔에서 **기본 «사용함»** 이라 `KAKAO_CLIENT_SECRET` 이 필요하다(「카카오 로그인」 줄 코드).
  동의항목 「카카오톡 메시지 전송」 = 이용 중 동의(연결할 때 `scope=talk_message` 로 요청하므로 그대로 된다)
  🔑앱의 「친구 목록」 권한은 **막혀 있지 않다**(테스트에서 이건호 친구 목록 조회 성공)
- ✅ **운영 배포 `c7ce2b8`** — 운영 `.env` 네 줄(Redirect **8082**, `KAKAO_SENDER`, `KAKAO_TO_TEST` 없이),
  백업 `.env.bak-20260925-kakao` · `.env.bak-20260925-sender`. 운영은 `friend`(이건호 → 고광용)
- ⏳ **친구 메시지 실제 발송은 월요일 사무실에서**(사용자 결정) — ① 대표이사를 카카오 콘솔 [앱]→[멤버] 에 초대·수락
  ② 운영 8082 에서 **이건호·대표이사 각자 [내 카카오 계정 연결]** ③ [친구 목록 확인] 「보입니다 ✓」 ④ 경비 1통
- 🔑 `.env` 를 바꾸면 **컨테이너를 다시 만들어야** 읽는다 — `docker compose -p <프로젝트> up -d --no-build --force-recreate backend`

## 프로젝트 손익 — 대표이사 전용 (2026-09-25 신설, 마이그 `044`~`046`)
탭 키 = `profit`(표기 「손익」). **대표이사 화면에만** 11번째 탭으로 보인다.

- 🔴**막는 기준은 «직책 대표이사»**(`isBossUser` · `requireBoss`) — `can_approve_settlement` 가
  아니다. 그것으로 막으면 승인 권한이 있는 이건호 계정에 전 직원 단가가 보인다.
  서버가 응답 자체를 403 으로 거절한다(화면 숨김만으로는 안 된다). `/api/auth/me` 가 `is_boss` 를 준다
- 손익 = **계약(공급가) − (인건비 + 경비 + 구매 + 이동)**. 🔑**금액 기준은 공급가**(지시)
  - 계약 = `vat_included` 체크 → `supply_amount` 를 **계산해 저장**(나중에 규칙이 바뀌어도 소급 안 됨)
  - 경비·구매 = 들어온 금액이 부가세 포함 → **÷1.1 공급가**로 원가, 포함 금액은 화면에 함께
    («잘 구별해서 볼 수 있게» — 지시)
  - 이동 = 실적의 `transit_fee + toll_fee + fuel_fee` **실지출만**(지시 — 거리×단가 아님).
    면세가 섞여 나누지 않는다. ⚠ 하이패스는 이미 `toll_fee` 에 들어 있다 — 따로 더하면 두 번 센다
  - 인건비 = `work_history` 시간 × **그날 적용되던 단가**(`effective_from`). 단가 없는 시간은
    0원으로 잡고 **`missing_rates` 로 붉게 알린다**
  - 구매 = **승인된 것만**, 날짜는 `coalesce(approved_at, created_at)`
- **계약은 이력** — 바꿀 때마다 한 줄(`kind` initial/change), 현재 = 최신 줄
- 이동 비용 배분 = 계획의 `projects` 비율. 직접 적은 몫 · 프로젝트 없는 계획 · 계획 없는 실적 ·
  고정업무 → **「공통(간접)」 줄**
- 구매에 `parent_key`/`parent_text`(선택). `PATCH /api/purchases/:id/project` 로 **승인 뒤에도** 바꾼다
- ⚠ 기간을 좁히면 **원가만** 잘린다(계약은 전체 금액) — 화면에 경고를 붙였다
- ⚠ `BIGINT` 는 문자열로 온다 — 계산 전에 `Number()`
- 테스트 서버 확인은 테스트 DB 에서만 이건호 직책을 잠깐 「대표이사」 로 바꿨다 되돌린다

## 회의록과 안건 (2026-09-04·05 · 07·08 확장)
「안건」 탭을 **회의록**으로 올렸다. **탭 키는 `agenda` 그대로** 둔다(주소·북마크 유지).

- 표 = `meetings`(제목·날짜·장소·참석자·**작성자**·본문) / `agenda_items`(그 아래 안건) /
  `meeting_notes`(**참석자별 발표 내용** — 마이그 `036`·`037`)
- 화면이 **「✍ 회의록 작성」 / 「📚 지난 회의록 조회」** 로 갈려 있다 —
  회의 중에 지난 목록이 화면을 밀어내지 않게 하기 위함이다. 조회는 **달력**이 주인이다
- 🔑**안건은 «회의록을 시작한 뒤에만» 적는다** (2026-09-05 지시).
  회의록이 없으면 「안건 등록」 카드를 아예 내지 않는다. 회의 없이 안건을 «새로 만드는»
  길은 없앴다 — 「회의 없는 안건」은 예전 것과 회의록이 지워진 것을 «보는» 자리로만 남는다
- ⚠**회의록을 지워도 안건은 남는다** — 기한·담당·Jira 가 걸려 있다
- 안건 등록은 **①프로젝트 → ②안건** 순서. 프로젝트는 업무 입력과 «같은 목록» 을 쓴다
- **보고자**(`reporter_worker_id`, 마이그 `034`) — 「맡아 하는 사람(담당자)」과
  「회의에서 말하는 사람」이 다를 수 있다. 🔑Jira 계정 매핑은 «하지 않는다»(품이 크다) —
  Jira 로 올릴 때 **제목 뒤에 `_이름`** 을 붙인다(다시 올려도 겹쳐 붙지 않는다)
- **작성자**(`author_worker_id` / `author_text`, 마이그 `036`) — 「실제로 적은 사람」.
  🔴`created_by` 는 «누가 넣었나» 로 그대로 둔다 — **지우기 권한이 거기 걸려 있어**
  뜻을 바꾸면 남의 회의록을 지울 수 있게 된다

### 서식 있는 본문 — 두 겹으로 거른다 (2026-09-07)
- `body_html` 이 주인이고 `body` 는 **태그를 뗀 사본**이다(검색이 그것을 훑는다).
  `body_html` 이 비면 화면은 옛 글자를 그대로 보여 준다 — 지난 회의록이 살아 있어야 한다
- 편집기는 `contentEditable` + `document.execCommand`. **새 꾸러미를 들이지 않았다**
- 🔴🔑**보는 자리만 막으면 안 된다.** 서버(`cleanHtml`)와 화면(`sanitizeHtml`) 두 겹인데,
  **「내용 수정」을 누르는 순간** `<img onerror>` 가 터졌다(2026-09-07 실측).
  `innerHTML` 은 `<script>` 는 안 돌리지만 **`onerror` 는 돌린다** → `RichEditor` 가
  «담을 때도» 거른다. 🔑잡은 법 = **DB 에 직접 심고 눌러 보기**(서버가 걸러 주니
  API 로만 시험하면 «화면을 잰 것이 아니다»)
- 붙여 넣기는 **글자만** 받는다 — 워드·웹의 배경색·글꼴이 따라오면 흰 바탕에 흰 글씨가 된다

### 참석자별 발표 내용 (2026-09-07·08, 마이그 `036`·`037`)
「컨플루언스처럼 여럿이 동시에」는 실시간 협업 편집이라 감당이 안 된다 →
🔑**사람마다 «자기 칸»** 으로 갈랐다(사용자 채택). 부딪힐 일이 없고, 읽을 때는
본문 아래에 이어 붙여 **«한 박스»** 로 보인다.

- 한 줄 = **«한 회의 · 한 사람 · 한 프로젝트»**. 프로젝트는 안건과 «같은 목록»(상위업무 하나)
- 화면에 **[🗣 인원별] / [📁 프로젝트별]** — **담긴 글은 하나이고 «묶는 기준»만 바뀐다**
- 판정은 «주소의 `worker_id`»와 세션을 맞대 한다(본인만, 관리자는 대리 입력)
- 🔴`ON DELETE CASCADE` — `agenda_items` 와 **반대**다. 발표 내용은 «회의록 본문의 일부»라
  본문이 지워지는데 조각만 떠돌면 누구 것인지도 무슨 회의였는지도 알 수 없다
- 🔴🔴**`UNIQUE (회의, 사람, 프로젝트)` 로는 «안 막힌다»** — `NULL` 끼리는 서로 같지 않아
  프로젝트를 안 고른 줄이 얼마든지 쌓인다. 하이패스 `CHECK` 함정과 **같은 뿌리**(3값 논리)
  → `coalesce(parent_text,'')` 로 잠갔다
- 🔑프로젝트를 «옮기면» 옛 줄을 **먼저** 지운다(안 그러면 같은 글이 두 자리에 남는다).
  옮겨 갈 자리에 이미 있으면 덮어쓰기 전에 물어본다
- ⚠**고른 값을 프로젝트 목록에 «남겨» 둬야 한다** — 목록은 완료한 것을 감추므로,
  완료된 프로젝트로 적어 둔 것을 열면 고르개가 조용히 빈칸이 되고 저장하는 순간 날아간다
  (안건에서 이미 겪은 함정)

## 로그인과 「내 것만 수정」 (2026-08-21 전면 적용)
**화면 «전체»가 로그인 뒤에 있다.** 2026-08-21 이전에는 정산 화면만 막고 달력·업무 입력은 열어 두었는데
(「요구하면 아무도 안 적는다」), 재직자 8명 전원에게 계정이 생겨 그 걱정이 없어졌고
**누가 적었는지 확인되지 않으면 남의 기록을 고칠 수 있다**는 문제가 더 컸다.

계정·세션은 **KPI 추적 시스템과 공유** — 같은 `kpi_users`·같은 `SESSION_SECRET`·쿠키 `kpi_session`.
KPI(:8083)에서 로그인했으면 대시보드(:8082)도 그대로 열린다.

- 🔑**게이트는 라우트마다 붙이지 않고 「전부 막고 예외만 여는」 `app.use` 하나로 둔다** —
  붙이는 방식은 새 API 를 더할 때 조용히 빠뜨리고, 빠뜨려도 아무 증상이 없다.
  여는 것은 `/api/health` · `/api/auth/login|logout|me` 넷뿐
- 🔑**수정·삭제 판정은 «본문»이 아니라 «DB 의 `worker_id`»로** 한다.
  본문을 믿으면 남의 계획을 열어 자기 번호를 적어 보내는 것으로 뚫린다
- **본인만 수정** = 업무 기록(`/api/history/save`) · 스케줄 계획·실적.
  **`role='admin'` 은 통과**(대리 입력) — 장기 부재자 기록을 넣어 줄 길이 없으면 운영이 막힌다
- 설정 탭(직원·차량·장소·집계 제외·고정업무·Jira 동기화)은 **로그인만 하면 누구나** (2026-08-21 결정)
- ⚠️ **임시 비밀번호(`must_change_password`) 계정은 들여보내지 않고 KPI 로 보낸다.**
  비밀번호를 다루는 코드가 두 벌이 되면 반드시 어긋나므로 「바꾸는 곳」은 KPI 하나뿐이다
- ⚠️ 쿠키에 `Secure` 금지 (사내는 `http` — 붙이면 쿠키가 저장되지 않아 로그인 불가)
- ⚠️ `.env` 의 `SESSION_SECRET` 이 KPI 와 **같은 값이어야** 세션이 공유된다. 없으면 재시작마다 전원 로그아웃
- 정산 확정 권한은 등급이 아니라 `kpi_users.can_approve_settlement` 컬럼 (일반 관리자에게 비용 승인 권한을 주지 않는다)

## 집계 제외 (장기 부재·대상 아님)
장기 출장자는 업무를 입력할 수 없는데 재직자로 세어져 1인 평균을 끌어내리고 최소값·순위를 흔들었다.
대표이사처럼 애초에 대상이 아닌 사람도 같은 문제다. **사람을 지우지 않고 「그 기간만」 뺀다.**

- 테이블 `worker_absences` (마이그레이션 `005`·`006`), API `GET/POST/PATCH/DELETE /api/worker-absences`
- 사유 4가지 = 장기출장 / 휴직 / 파견 / **집계 제외**(부재가 아니라 「대상 아님」 — 기간을 열어 둔다)
- 🔑**가동일 = 영업일 − 재직 기간 밖 − 부재일.** 모든 비율의 분모가 이것이다
- 부재 기간의 기록도 **집계에서 빼되 원본은 보존**한다
- 🔑**입력표에 세우는 기준이 사유마다 다르다** —
  「집계 제외」는 **업무 입력표에서도 뺀다**(애초에 적을 사람이 아니라, 빈 칸이 미입력으로 보인다).
  장기출장·휴직·파견은 **그대로 세운다**(돌아와 그 기간을 소급 입력할 수 있어야 한다).
  스케줄·설정 탭은 사유와 무관하게 전원 보인다(차량·일정은 쓰고, 설정에서 보여야 고친다)
- ⚠️ **가동일 0인 사람은 평균·최대·최소·순위에서 제외** — 남기면 0시간이 최소값으로 고정된다.
  비교는 전부 **하루당**(열흘만 있었어도 그 열흘을 남들만큼 했으면 불이익 없음)
- 등록은 [설정] 탭 「집계 제외」 카드. **KPI 추적 시스템이 사유를 가리지 않고 같은 표를 읽으므로**
  한 번 등록하면 양쪽(`:8082`·`:8083`) 숫자가 함께 바뀐다
- 문서 = Confluence 「[대시보드] 11. 집계 제외」(171147281) · 산식 정본은 「[KPI] 4. 점수 계산 규칙」 4.9 절

## Jira 동기화
- 프론트 → 백엔드 `POST /api/jira-sync` → Jira REST API → PostgreSQL
- **상태도 함께 받는다** (`status_name`·`status_category`, 마이그레이션 `007`).
  🔑판정은 **반드시 `status_category`**(`new`/`indeterminate`/`done`) 로 — 표시 이름(「완료」·「검토 중」)은
  프로젝트 설정에 따라 바뀌어 그것으로 거르면 조용히 어긋난다

### 종료 업무와 고정업무 (업무 입력 화면)
- 종료(`done`)한 업무는 **고르는 목록에서 기본으로 감춘다.** 「완료 포함」 체크로 다시 꺼낼 수 있다 —
  완료 처리 뒤에도 보완 작업이 이어지는 일이 실제로 있다(최근 30일에도 완료 업무에 76건이 적혔다)
- 목록에 보일 때는 앞에 **`(완료) `** 를 붙인다.
  🔴**저장값은 원본 `full_text` 그대로** — 표시 문구를 저장하면 상태가 바뀔 때마다 같은 업무가 갈라진다
- 🔑**이미 골라져 있는 값은 완료여도 목록에 남긴다.** 빼면 과거 날짜 조회 시 적어 둔 업무가 빈칸이 된다
- **고정업무** = Jira 에 넣기 애매한 반복 업무(주간회의 등). 상위업무 **`고정업무`**(상수 `FIXED_PARENT`) 아래에 모은다.
  내부적으로는 수동 추가(`MANUAL-…`)와 같아 **동기화가 지우지 않는다**. 상태가 NULL 이라 항상 보인다.
  등록은 [설정] 탭 「고정업무」 카드
- 토큰은 서버 환경변수에만 있으므로 **로그인 없이 누구나 동기화 가능**
- 검색은 신형 `/rest/api/3/search/jql` + `nextPageToken` 페이지네이션
  (구형 `/rest/api/3/search` 는 Atlassian 이 제거해 410)
- 조회 0건이면 **기존 목록을 지우지 않고 중단** (전량 삭제 사고 방지)
- 페이지 100장·이슈 2만 건 상한 + 토큰 반복 감지 — 메모리 고갈 방지
- 동기화 중 토스트는 `duration=0`으로 완료 전까지 계속 표시

### Jira API 토큰
- Atlassian 정책상 **최대 유효기간 365일**, 무기한 토큰 없음
- 만료일 조회 API 가 없어 `.env` 의 `JIRA_TOKEN_EXPIRES=YYYY-MM-DD` 에 직접 기록
- 만료 30일 전부터 화면 상단 배너 + 설정 탭에 남은 일수 표시
- 갱신 시 `.env`(NAS 는 `/volume1/docker-build/.env`) 교체 후 `./deploy.sh`

## 개발 서버
백엔드와 프론트를 **둘 다** 띄워야 한다. Vite 가 `/api` 를 3001 로 프록시한다.
```powershell
cd server; node index.js   # 백엔드 :3001 (.env 는 프로젝트 루트에서 읽음)
npm run dev                # 프론트 :5173
```
`.claude/launch.json`에 서버 설정 있음 — Preview 도구로 브라우저 테스트 가능.

## 배포 (사내 NAS)
**PC 에서 이 한 줄이면 끝난다.** (압축 → 전송 → 원격 빌드·교체까지 자동)
```powershell
.\push-to-nas.ps1          # 운영  http://vitron-nas:8082
.\push-to-nas.ps1 -Test    # 테스트 http://vitron-nas:8092  (DB 도 vitron_dashboard_test 로 다르다)
.\push-to-nas.ps1 -Force   # 커밋 안 된 변경이 있어도 묻지 않는다
```
- 🔑**테스트 서버가 따로 있다.** 같은 소스·다른 `.env` 세 줄(포트·이미지 태그·DB 이름)로
  뜬다. 거기서 넣고 지운 것은 운영에 반영되지 않고, 화면 맨 위에 붉은 띠가 뜬다
- ⚠ 대화형이 아닌 셸에서는 **`-Force` 를 함께** 준다. 커밋 안 된 파일이 하나라도 있으면
  스크립트가 `Read-Host` 로 물어보다 그대로 멈춘다 (2026-09-05 실제로 겪음)
- 커밋된 내용(git HEAD)만 배포된다. 미커밋 변경이 있으면 경고하고 확인을 받는다
- 비밀번호를 두 번(scp·ssh) 묻는다. SSH 키를 등록하면 생략된다
- NAS 소스 폴더 `/volume1/docker-build`, 실제 빌드는 그 안의 `deploy.sh` 가 수행
- 수동으로 할 경우: `git archive --format=tar.gz -o vitron-src.tar.gz HEAD` → `scp`
  → NAS 에서 `tar -xzf vitron-src.tar.gz && ./deploy.sh`

### 배포 관련 주의사항
- NAS 에는 `git` 이 없다 (그래서 파일 전송 방식)
- Portainer 스택(`vitron-dashboard`)으로 등록돼 있고 컨테이너 이름을 이어받는다.
  Portainer 화면에는 Editor 가 안 뜨므로 **갱신은 항상 위 스크립트/`deploy.sh` 로 한다**
  (Portainer UI 에서 스택을 Update 하면 옛 정의로 되돌아갈 수 있다)
- `.env` 는 git 에 없다. NAS 의 `/volume1/docker-build/.env` 를 그대로 유지한다
  (tar 에 포함되지 않으므로 배포해도 덮어써지지 않는다)
- ⚠️ **`.ps1` 은 UTF-8 BOM 으로 저장해야 한다.** BOM 이 없으면 PowerShell 5.1 이
  ANSI 로 읽어 한글이 깨지고 파싱 오류가 난다
- ⚠️ **`.sh` 는 LF 로 유지해야 한다.** CRLF 면 NAS 에서 bad interpreter 오류가 난다
  (`.gitattributes` 로 고정해 둠)

## 주의사항
- `App.jsx`는 의도적으로 단일 파일 구조 유지 (분리 금지).
  ⚠ 예외 = `src/shared/` — 스케줄 달력은 «사내 포털과 함께 쓰는» 조각이라 여기 두지 않는다
- **탭 구성 10개**: 오늘 업무 / 일간 / 주간 / 월간 / 연간 / **스케줄** / **회의록** / **구매** / **경비** / 설정
  — **대표이사에게는 「손익」 이 더해져 11개**(경비와 설정 사이, `canTabFor`)
  - `TABS` 의 키는 `schedule`(표기 「스케줄」 — 문서에서는 「일정」으로도 부른다),
    **`agenda`(표기 「회의록」 — 2026-09-05 에 「안건」에서 올렸으나 탭 키는 그대로 둔다)**,
    `purchase`(표기 「구매」), `expense`(표기 「경비」 — 2026-09-24 신설),
    `profit`(표기 「손익」 — 2026-09-25 신설, 대표이사 전용)
  - 🔑 탭 개수를 세는 문서가 여럿이다(Confluence 3. 화면 구성, 교육 자료 그림 3-1).
    탭을 더하면 **그것들도 함께** 고칠 것
- 직원 필터는 기간별로 재직 여부를 판단 (`workersForPeriod` 함수)
- ⚠️ **스키마 변경은 `admin` 계정으로** — 앱 계정(`vitron-dashboard`)은 테이블 소유자가 아니라
  `ALTER TABLE` 이 거부된다. 새 테이블을 만들었으면 앱 계정에 `GRANT` 하는 것을 잊지 말 것
  (안 하면 화면이 권한 오류로 조용히 비어 보인다)
- ⚠️ **배포 검증은 브라우저에서 버튼까지 눌러 볼 것.** API E2E 만 돌리고 끝냈다가 놓친 사고가 있다
